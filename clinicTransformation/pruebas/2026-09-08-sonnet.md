# Prueba — Sonnet 5, 2026-09-08

Input: `ejemplos/excelEvaluacionCompleto.xlsx` (paciente real).

## Observación 1 — Bloque 1 (datos personales) se pega corrido

**Síntoma:** al copiar el bloque de datos personales y pegarlo en Word, el texto queda todo seguido
sin separación entre campo y valor ni salto de línea entre filas:

> PacienteBunader, José AlbertoEdad61 añosFecha de nacimiento08/09/1964Nivel educativo
> alcanzadoTerciarioLateralidadDiestroFecha de evaluación31/08/2026

**Causa probable:** el bloque se emitió como tabla Markdown; al copiarla desde el chat, Word no
respeta la estructura de filas/columnas (mismo problema que ya se había detectado y resuelto para el
bloque 3, la tabla de síntesis).

**Fix aplicado:** `SKILL.md` §1 ahora especifica formato **HTML** para el bloque de datos personales
(tabla simple de 2 columnas, sin sombreado necesario), mismo mecanismo de paste que el bloque 3.
`ejemplo-informe.md` actualizado con el ejemplo en HTML.

**Pendiente de confirmar:** falta volver a correr la skill con este fix y verificar en Word real que
el paste queda en filas separadas.

## Observación 2 — comentario sobre el estado del dato metido en la prosa de anamnesis

**Síntoma:** en la frase de quejas subjetivas de memoria, la skill escribió:

> En relación a quejas subjetivas de memoria, menciona que olvida cosas puntuales — "fue a un partido
> y por ahí…" (nota incompleta en el registro original, sin continuación).

El paréntesis final es un comentario sobre la calidad del dato de origen (`B58` truncada en el Excel),
no contenido clínico — y el paciente puede leer este informe. No corresponde que aparezca ahí.

**Corrección pedida:** dejar sólo `En relación a quejas subjetivas de memoria, menciona que olvida
cosas puntuales.` (la cita textual truncada se puede incluir o no según corresponda, pero sin
explicar el motivo del corte).

**Fix aplicado:** `SKILL.md` — la convención de marcado de pendientes ahora incluye este caso como
tercer ejemplo ❌ explícito (un comentario sobre el estado del dato metido en la prosa es tan
inapropiado como un `[PENDIENTE]`, aunque no use esa palabra). El aviso del bloque de anamnesis
("bloque de mayor riesgo") ahora aclara que "señalarlo" es **sólo en el bloque 11**, nunca como
aclaración dentro de la prosa que lee el paciente.

## Observación 3 — pedido de cambio: sacar "N/A" y fusionar la columna ÁREA

Dos cambios pedidos por el usuario para la tabla SÍNTESIS DEL RENDIMIENTO (no son bugs, son ajustes de
diseño):

1. **Sacar el texto `N/A`** de las celdas que no llevan puntaje Z — dejarlas vacías, conservando sólo
   el fondo gris `#D9D9D9` (así lo hacía la plantilla Word original; la skill había agregado el texto
   como sustituto del color para cuando la salida era texto plano, pero en HTML el color ya sobrevive
   el paste, así que el texto quedó redundante y sólo ensanchaba las columnas).
2. **Columna `ÁREA` con celdas fusionadas (`rowspan`)** en vez de repetir el nombre en cada una de las
   36 filas, como en el informe final de referencia.

**Fix aplicado:**
- `SKILL.md` y `orden-filas-sintesis.md`: convenciones de llenado, autochequeo y conteos de control
  actualizados a este esquema.
- `ejemplos/generar-tabla-sintesis.js`: `rowspan` en la celda `ÁREA` (sólo en la primera fila de cada
  uno de los 5 grupos) y las celdas de "no aplica" ahora van vacías (antes tenían el texto `N/A`).
- `ejemplos/ejemplo-tabla-sintesis.html` regenerado con el generador actualizado.

**Pendiente:** el `rowspan` es un mecanismo nuevo, sin probar en Word real todavía (a diferencia del
resto de la estructura, que ya estaba verificada). Falta pegar la tabla regenerada en Word y confirmar
que la celda de ÁREA fusionada se comporta bien.

**Nota aparte (no pedida, detectada al revisar):** el generador de referencia sombrea con `#D9D9D9`
las 8 celdas de rango de AVD/KPDS-10, mientras que la plantilla Word original **no** las sombrea (ver
mapa de sombreado en `orden-filas-sintesis.md`). Se documentó como diferencia deliberada (mismo
criterio visual que las filas cualitativas), no se resolvió activamente — no afecta lo que se informa.

## Observación 4 — receta de ajuste de ancho, confirmada por el usuario

Probando la tabla de síntesis pegada en Word, el usuario encontró que **Autoajustar al contenido +
Ancho preferido 130%** (Propiedades de tabla → pestaña Tabla) resuelve el apriete de márgenes sin
tocar el documento ni el HTML.

**Fix aplicado:** agregado como instrucción a incluir en la salida (bloque 11 y junto al bloque 3) en
`SKILL.md`, y en el paso 3 de "Cómo la usa la profesional" de `orden-filas-sintesis.md`. La skill
ahora debe sugerir este ajuste al entregar la tabla de síntesis en HTML.
